#include<stdio.h>
int main()
{
    int t,n,x;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
        if(n==1)
        {
            printf("\n%d",1);
        }
        else
        {
            x=n/2+1;
            printf("\n%d",x);
        }
    }
}
