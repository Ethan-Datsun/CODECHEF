#include<stdio.h>

int main() 
{

int t,n,sum=0,a[100000],i;
	scanf("%d",&t);
	while(t--)
	{
	    scanf("%d",&n);
	    sum=0;
	    for(i=0;i<n;i++)
	    {
	        scanf("%d",&a[i]);
	        sum+=a[i];
	    }
	    if(sum%2==0)
	    printf("\n1");
	    else
	    printf("\n2");
	}
	return 0;
}
